import React, { PropTypes } from 'react';

class Dashboard extends React.Component{
  constructor(props) {
    super(props);
  }

  render(){
    return (
      <h1>Dashboard</h1>
    );
  }
}

Dashboard.propTypes = {
};

export default Dashboard;
